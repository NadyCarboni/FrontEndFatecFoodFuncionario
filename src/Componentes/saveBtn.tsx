import React from "react";
import { useForm } from "react-hook-form";

export default function SaveBtn() {
  return (
    <button type="submit" className="saveBtn titleGrad1 poppins">
      Salvar
    </button>
  );
}
